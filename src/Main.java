/**
 * Die Klasse {@code Main} dient dem Einstieg in das Programm.
 * Hier werden die notwendigen Klassen aufgerufen, die zur Bearbeitung des Problems notwendig sind.
 * 
 * @author Lukas Dahlberg
 * @version 1.0
 */
public class Main{
  
  public static void main(String[] args){
    if(args.length > 2){
      for(int i = 2; i < args.length; ++i){
        // Aufruf des Controller Interface mit der Übergabe der Eingabedatei in args[i]
      }
    }
  }
  
}
